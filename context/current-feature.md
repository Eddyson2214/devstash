# Current Feature
<!--Feature name and short description -->

## Status

## Goals
<!-- Goals and requirements -->

## Notes
<!-- Any extra notes -->

## History
<!-- Keep this updated. Earliest to latest -->
- 2026-07-04 - Initial Next.js and Tailwind setup committed (`chore: initial next.js and tailwind setup`) and pushed to `origin/master` at https://github.com/Eddyson2214/devstash.git.
- 2026-07-05 - Dashboard UI Phase 1 (ShadCN setup, /dashboard route, placeholder layout) implemented and verified on `feature/dashboard-phase-1`.
- 2026-07-05 - Dashboard UI Phase 2 (collapsible sidebar, item type links to /items/[type], favorite/recent collections, user avatar footer, mobile drawer) implemented on `feature/dashboard-phase-2`; build and lint verified.
- 2026-07-05 - Dashboard UI Phase 3 (stats cards, recent collections grid, pinned items, 10 recent items) implemented on `feature/dashboard-phase-3`, merged to `master`, and pushed to `origin/master`.
- 2026-07-07 - Prisma 7 + Neon Postgres setup (schema with NextAuth models, prisma.config.ts, PrismaNeon adapter client, dev/production Neon branches, system ItemType seed script) implemented on `feature/prisma-neon-setup`; migration applied and verified against the Neon `development` branch, build and lint passing.
- 2026-07-07 - Seed data (demo user with bcryptjs-hashed password, 7 system item types, 5 collections with 18 items) implemented on `feature/seed-data`; idempotent re-seed verified, build and lint passing.
- 2026-07-08 - Dashboard Collections (real Recent Collections data via new `src/lib/db/collections.ts`, fetched directly in the `RecentCollections` server component, border accent + type icons derived from the most-used item type per collection) implemented on `feature/dashboard-collections`; build and lint passing.
- 2026-07-08 - Dashboard Items (real pinned/recent item data via new `src/lib/db/items.ts`, fetched directly in the `DashboardPage` server component, item card icon/border derived from item type, pinned section hidden when empty, stats cards wired to live item/collection counts via new `getCollectionStats`) implemented on `feature/dashboard-items`; build and lint passing.
- 2026-07-09 - Stats & Sidebar (sidebar item types, favorite/recent collections, and "View all collections" links wired to Neon data via new `getItemTypesWithCounts` in `src/lib/db/items.ts` and new `getFavoriteCollections` in `src/lib/db/collections.ts`; recents show a colored circle by most-used item type, favorites keep the star badge; `AppSidebar` now receives data as props from `DashboardPage`; removed now-unused mock collections/items from `src/lib/mock-data.ts`) implemented on `feature/stats-sidebar`.
- 2026-07-10 - Add Pro Badge to Sidebar (subtle uppercase "PRO" badge using the ShadCN `Badge` component, shown next to the File and Image item types in the sidebar's Types list, hidden when the sidebar is collapsed) implemented on `feature/add-pro-badge-sidebar`; build and lint passing.
- 2026-07-17 - Code Scan Quick Wins (dashboard components receive data as props instead of re-fetching, `dashboard/loading.tsx` + `error.tsx` added, `db/sampledata.tsx`/`db/typesystem.tsx` moved to `prisma/*.ts`, item-type slug logic deduped via `typeHref`, seed script writes batched with `Promise.all`/`createMany`, `aria-label`/`aria-hidden` added across dashboard components, weak demo password replaced; also fixed a pre-existing `npm run build` failure by marking `/dashboard` `force-dynamic`) implemented on `feature/code-scan-quick-wins`; build, lint, and browser verified.
- 2026-07-17 - Auth Setup - NextAuth + GitHub Provider (installed `next-auth@beta` + `@auth/prisma-adapter`; split `src/auth.config.ts`/`src/auth.ts` for edge compatibility with JWT session strategy; GitHub OAuth provider; `src/app/api/auth/[...nextauth]/route.ts` handlers; `src/proxy.ts` protecting `/dashboard/*` and redirecting unauthenticated users to NextAuth's default sign-in page; `src/types/next-auth.d.ts` extending `Session.user.id`) implemented on `feature/auth-setup`; build, lint, and browser verified (redirect-to-signin flow and GitHub button confirmed; full OAuth round-trip not exercised, requires a real GitHub account).
- 2026-07-18 - Auth Credentials - Email/Password Provider (Credentials provider placeholder in `src/auth.config.ts`; real bcrypt-based `authorize` override in `src/auth.ts`, returning only `{id, name, email, image}` to avoid leaking the password hash into the JWT; new `POST /api/auth/register` route with Zod-validated input, duplicate-email check, bcryptjs hashing at 12 rounds; installed `zod` per coding-standards.md) implemented on `feature/auth-credentials-email-password`; build, lint, curl, and browser sign-in verified (GitHub OAuth still functional).
- 2026-07-18 - Auth UI - Sign In, Register & Sign Out (custom `/sign-in` and `/register` pages replacing NextAuth's default pages; new `src/actions/auth.ts` Server Actions for credentials sign-in, GitHub sign-in, and sign-out; new reusable `UserAvatar` component (GitHub image or initials fallback); `AppSidebar` footer now shows the real session user in a dropdown with Profile/Sign-out; new minimal `/profile` page; `proxy.ts` now protects `/profile` too and redirects to `/sign-in`; added shadcn `dropdown-menu` and `label` components; removed the now-dead mock `currentUser`) implemented on `feature/auth-ui-signin-register-signout`; build, lint, and full browser walkthrough of all 7 testing-plan steps verified.
- 2026-07-18 - Auth Toast Notifications (added `sonner` + new `src/components/ui/sonner.tsx` Toaster, hardcoded to dark theme; new `AuthToast` client component mounted once in the root layout, reading `?auth=`/`?error=` query params via `useSearchParams` and firing a toast for login/logout/register success or any failed auth attempt, then stripping those params from the URL; `src/actions/auth.ts` now appends `?auth=login-success`/`?auth=logout-success` to redirect targets; `RegisterForm` now uses `toast.error(...)` instead of inline text; removed the now-redundant inline error/success paragraphs from `/sign-in`) implemented on `feature/auth-toast-notifications`; build, lint, and browser-verified for all 6 directly-testable scenarios (login success/failure, logout, register success/failure/duplicate-email) by inspecting `[data-sonner-toast]` directly since toasts auto-dismiss quickly; caught and fixed a bug where the toast effect's empty dependency array meant it only fired once per layout mount instead of on every auth-redirect.
- 2026-07-18 - Email Verification on Register (installed `resend`; new `src/lib/email.ts` + `src/lib/verification-token.ts` issuing 24h tokens via the existing `VerificationToken` model; `POST /api/auth/register` now sends a verification email after user creation; new `GET /api/auth/verify-email` route validates the token, sets `User.emailVerified`, deletes the token, and redirects with an `email-verified` toast marker; GitHub OAuth sign-ups auto-verified via a custom `profile()` callback on the GitHub provider in `src/auth.ts`; `session` callback now exposes live `emailVerified` status, extended in `src/types/next-auth.d.ts`; sign-in is NOT gated on verification (decision confirmed with user) — instead a new `VerifyEmailBanner` on the dashboard shows a persistent reminder with a "Resend email" button wired to a new `resendVerificationEmail` server action; new `invalid-token`/`expired-token`/`verification-sent` toast messages) implemented on `feature/email-verification-on-register`; build and lint passing, full browser walkthrough via Playwright verified (register → email issued → verify link → banner for unverified sign-in → resend → re-verify → banner cleared, plus the invalid-token error path), test accounts cleaned up from the Neon `development` branch afterward.
- 2026-07-18 - Email Verification Feature Flag (new `src/lib/feature-flags.ts` with `isEmailVerificationEnabled()`, reading `EMAIL_VERIFICATION_ENABLED` from `.env`, default enabled unless explicitly `"false"`; `POST /api/auth/register` now skips creating a `VerificationToken`/sending the Resend email and sets `emailVerified` immediately on the new user when the flag is off; `resendVerificationEmail` in `src/actions/auth.ts` short-circuits with a clear message when the flag is off, as a safety net) implemented on `feature/email-verification-flag`; motivated by Resend having no verified sending domain yet (sandbox sender can only deliver to the account owner's own email); build and lint passing, browser-verified both flag states end-to-end (flag off: user auto-verified, no token row, no banner; flag on: unchanged token/banner flow), `.env` left with `EMAIL_VERIFICATION_ENABLED=false` since no domain is verified yet, test accounts cleaned up from the Neon `development` branch afterward.
- 2026-07-18 - Forgot Password (new "Forgot password?" link on `/sign-in`; new `/forgot-password` page + `ForgotPasswordForm` backed by a new `requestPasswordReset` server action that always returns a generic success response to avoid user enumeration and no-ops for GitHub-only accounts; new `/reset-password` page + `ResetPasswordForm` backed by a new `resetPassword` server action validating the token/expiry, bcrypt-hashing the new password at 12 rounds, and deleting the token; new `issuePasswordResetToken` in `src/lib/verification-token.ts` reusing the existing `VerificationToken` model with a shorter 1h expiry than email verification's 24h, and new `sendPasswordResetEmail` in `src/lib/email.ts`; new `reset-email-sent`/`password-reset-success`/`invalid-reset-token`/`expired-reset-token` toast messages) implemented on `feature/forgot-password`; decision confirmed with user not to add a `type`/`purpose` column to `VerificationToken` — reset and verification tokens share the same keyspace and can invalidate each other in the rare case both are pending for the same email, accepted as an edge case; build and lint passing, full browser walkthrough via Playwright verified against the Neon `development` branch (happy path including sign-in with the new password, password-mismatch validation, invalid/reused-token error, and missing-params guard on `/reset-password`), demo account's password restored to its original seeded value afterward.
- 2026-07-19 - Profile Page (new `/profile` route showing avatar (GitHub image or initials generated from name/email via updated `UserAvatar`), name, email, member-since date, item/collection usage stats, and a per-item-type breakdown, via new `src/lib/db/profile.ts`; new `src/actions/profile.ts` Server Actions for `changePassword` (bcrypt-verifies current password, only offered when `user.hasPassword`) and `deleteAccount` (cascades via existing `onDelete: Cascade` relations, signs out with an `account-deleted` toast marker); new `ChangePasswordForm` and `DeleteAccountDialog` components, the latter backed by a new shadcn `alert-dialog` component; new `ItemTypeBreakdown` component) implemented on `feature/profile-page`; build and lint passing, full browser walkthrough via Playwright verified (profile display against both the demo account and a disposable test account, change-password wrong-current-password error path and happy path re-verified by signing in with the new password, delete-account confirm/cancel dialog and full deletion re-verified by confirming sign-in fails afterward), test account cleaned up. Noted but out of scope: `getItemStats`/`getCollectionStats` in `src/lib/db/items.ts` and `collections.ts` still call a leftover `getDemoUser()` instead of the session user, so the dashboard stats cards show the demo account's counts regardless of who's signed in — the profile page itself correctly scopes to `session.user.id`.
- 2026-07-19 - Rate Limiting for Auth (new `src/lib/rate-limit.ts` wrapping `@upstash/ratelimit` + `@upstash/redis` with one sliding-window limiter per endpoint, each given its own `prefix` to avoid key collisions between limiters that share an `ip:email` identifier shape; `checkRateLimit` fails open — allows the request — if Upstash env vars are missing or a call errors; new `getClientIp` reads `x-forwarded-for`. Wired into: `POST /api/auth/register` (IP, 3/hour, real 429 + `Retry-After` header); the credentials `authorize()` callback in `src/auth.ts` (IP+email, 5/15min) via a new `RateLimitedSignin` class extending `CredentialsSignin` with `.type` set to a shared `rate-limited` code, since NextAuth's callback route isn't a route handler we control directly — `authorize`'s second argument is the raw `Request`, which carries the real headers; `requestPasswordReset`/`resendVerificationEmail`/`resetPassword` in `src/actions/auth.ts` (forgot-password IP 3/hour, resend-verification IP+email 3/15min, reset-password IP 5/15min), returning a friendly message instead of a status code since these are Server Actions; `src/lib/auth-errors.ts` and `ForgotPasswordForm` updated to surface the new `rate-limited` code/message. Simplified the spec's dynamic "try again in X minutes" wording to a static "Too many attempts. Please try again later." to fit the codebase's existing static-message error architecture. Build and lint passing; core sliding-window behavior verified against the real Upstash instance via an isolated throwaway test key (allowed 3, blocked the 4th/5th, sane `reset` timestamp); register's 429 + `Retry-After` verified end-to-end via curl with a synthetic `X-Forwarded-For` IP (allowed 3 registrations, blocked the 4th); login rate limiting verified end-to-end in the browser — 5 failed attempts still returned the normal "Invalid email or password", the 6th attempt was blocked with the rate-limit toast even using the **correct** password, proving the check runs before credential validation; forgot-password → reset-password → sign-in-with-new-password happy path re-verified unaffected on a fresh disposable account. Not exercised live: resend-verification's limiter, since `EMAIL_VERIFICATION_ENABLED=false` in this environment disables that flow entirely — covered instead by code review and the shared, already-validated `checkRateLimit`/`getClientIp` path. All disposable test accounts cleaned up from the Neon `development` branch afterward.
- 2026-07-25 - Items List View (dynamic route `/items/[type]` rewritten from its `mock-data.ts` placeholder to real Prisma-backed data; new `getItemTypeBySlug`/`getItemsByType` in `src/lib/db/items.ts`; new `src/components/items/ItemCard.tsx` (shadcn `Card`, left border + icon wash tinted by `itemType.color`, title/description/tags/date, pin/favorite indicators) and `ItemGrid.tsx` (responsive `grid-cols-1 md:grid-cols-2`, empty-state message); page keeps the same `AppSidebar`/`Topbar` shell as `/dashboard` so sidebar type links/counts stay live while browsing a type; now-unused `src/lib/mock-data.ts` removed) implemented on `feature/items-list-view`; build and lint passing, browser-verified via Playwright (populated grid for Snippets showing correct left-border color/icon/tags/date, live sidebar counts matching across all 7 types, empty state for Notes, 404 on an unknown type slug). Preceded by two `/research` docs, `docs/item-types.md` and `docs/item-crud-architecture.md`, which remain uncommitted pending the user's call on how to fold them in alongside other pre-existing uncommitted stray files (`CLAUDE.md`, `.mcp.json`, `.claude/agents/`, `context/research/`, `query_collections.mjs`) that were left out of this feature's commit to keep it focused.
- 2026-07-25 - Vitest Unit Testing Setup (`vitest` + `@vitest/coverage-v8` installed; `vitest.config.ts` runs in the `node` environment with native tsconfig path-alias resolution, no DOM/React plugin since components are out of scope; new `npm run test`/`test:watch`/`test:coverage` scripts; example tests establishing the pattern — `src/lib/auth-errors.test.ts` and `src/lib/rate-limit.test.ts` for pure utilities, `src/actions/profile.test.ts` for a Server Action with `@/auth`/`@/lib/prisma`/`bcryptjs` mocked via `vi.mock` — plus a new **Testing** section in `context/coding-standards.md`, workflow step 4 and the code-review checklist in `context/ai-interaction.md` updated to require `npm run test`, and `CLAUDE.md`'s commands list updated) implemented directly on `master`; build, lint, and the new test suite (13/13) all passing. Also fixed a pre-existing bug while editing `coding-standards.md`: an unclosed ` ```css ` fence in the Tailwind example was silently swallowing the rest of the file into one code block.
- 2026-07-25 - Items Grid Three Columns (`src/components/items/ItemGrid.tsx` grid classes changed from `grid-cols-1 gap-4 md:grid-cols-2` to `grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3`; no change to `ItemCard`) implemented on `feature/items-grid-three-columns`, merged to `master`; build and lint passing, browser-verified via Playwright at 390px (1 col), 800px (2 col), and 1440px (3 col) on `/items/snippets` and `/items/links`. No testable server-action/utility logic in scope for this change, per `/feature test`.
- 2026-07-25 - Item Drawer (right-side shadcn `Sheet` serving as the item detail view, no separate item page; new `getItemDetail(id, userId)` in `src/lib/db/items.ts` returning content, url, language, tags, and collections, ownership-scoped via `findFirst({ id, userId })`; new session-scoped `GET /api/items/[id]` route calling it after an `auth()` check; new client `ItemDrawerProvider`/`useItemDrawer` context holding open/selected-item state, rendered from both `/dashboard` and `/items/[type]` server-component pages; new `ItemDrawer` component with a loading skeleton, description/content-or-URL/tags/collections/details sections, and an action bar matching `context/screenshots/dashboard-ui-drawer.png` — Favorite/Pin/Edit/Delete render as disabled "Coming soon" stubs since no mutations were in scope, Copy is fully functional via `navigator.clipboard.writeText` with a toast; `ItemCard` and the dashboard's `ItemList` rows converted to client components and made clickable/keyboard-accessible to open the drawer) implemented on `feature/item-drawer`, merged to `master`; build and lint passing, `npm run test` 13/13 (no new tests — `getItemDetail` and the API route follow this codebase's established precedent of not unit-testing `src/lib/db/*.ts` or API routes, per `/feature test`), full browser walkthrough via Playwright verified against the Neon `development` branch (drawer opened correctly from both entry points, a link-type item showing its URL and a snippet-type item showing its code content, Copy toast confirmed via accessibility snapshot after `browser_find`'s text search initially missed it). Along the way, fixed an ESLint `react-hooks/set-state-in-effect` violation by deriving loading state from a `{ id, detail }` result tuple instead of calling `setState` synchronously in the fetch effect, and found that `npm run test` can intermittently fail with stale-module-resolution errors after `npm run build` runs in between — fixed by clearing `node_modules/.vite`. Also reconfirmed the pre-existing tech debt noted in the 2026-07-19 Profile Page entry: `getPinnedItems`/`getRecentItems`/etc. in `src/lib/db/items.ts` still key off a hardcoded demo user rather than the session user, which caused an initial false-alarm 404 when testing as a different signed-in account whose session didn't own the demo user's items — resolved for testing by signing in as `demo@devstash.io` instead of fixing the underlying tech debt, which remains out of scope.
- 2026-07-25 - Item Drawer — Edit Mode (Edit button in the drawer's action bar now swaps it for Save/Cancel and turns the view sections into controlled inputs instead of navigating away: Title/Description/Tags for every type, plus Content (snippet/prompt/command/note), Language (snippet/command), and URL (link) shown conditionally by item type name; item type, collections, and dates stay read-only display; Save disabled client-side when title is empty; new ownership-checked, Zod-validated `updateItem` Server Action in `src/actions/items.ts` backed by a new `updateItem` query in `src/lib/db/items.ts` that replaces tags via disconnect-all/connect-or-create and returns the updated `ItemDetail`, so the drawer refreshes from the action's response with no second fetch; `router.refresh()` called after save to keep the underlying card list in sync; added shadcn `Textarea` component; extracted a shared `toItemDetail` mapper in `src/lib/db/items.ts` to avoid duplicating the Prisma-result-to-`ItemDetail` mapping between `getItemDetail` and `updateItem`) implemented on `feature/item-drawer-edit`, merged to `master`; build and lint passing, new `src/actions/items.test.ts` (5 tests covering auth rejection, Zod validation of an empty title and an invalid URL, the not-found/ownership-failure path, and the happy path) brings the suite to 18/18; full browser walkthrough via Playwright verified against the Neon `development` branch (edited a snippet's title/tags and confirmed the drawer, underlying card list, and Updated date all reflected the change; confirmed a link item shows the URL field instead of Content/Language; confirmed the empty-title Save-disabled guard; confirmed Cancel discards changes and returns to view mode unmodified), test edits reverted afterward to keep seed data clean. Along the way, avoided a second ESLint `react-hooks/set-state-in-effect` violation by deriving `editing` from whether a `form.id` matches the current `itemId` (and clearing `form` in the `Sheet`'s `onOpenChange` handler) instead of resetting state from an effect.
- 2026-07-25 - Item Delete (Delete button in the drawer's action bar is now a shadcn `AlertDialog` trigger showing a confirmation with the item's title interpolated ("This permanently deletes '{title}'. This action cannot be undone."), Cancel + destructive Confirm actions, matching the pattern already used by `DeleteAccountDialog`; confirming deletes via a new ownership-checked `deleteItem` Server Action in `src/actions/items.ts` backed by a new `deleteItem` query in `src/lib/db/items.ts` — a single ownership-scoped `deleteMany({ where: { id, userId } })` returning a boolean, no separate `findFirst` needed — then closes both the confirm dialog and the drawer, shows a success toast, and calls `router.refresh()`; failure shows an error toast and leaves the drawer open; the `AlertDialog`'s open state is now controlled (`confirmingDelete`) so a successful delete can explicitly close it rather than leaving an orphaned dialog behind after the `Sheet` closes) implemented on `feature/item-delete`, merged to `master`; build and lint passing, `src/actions/items.test.ts` gained 3 cases (auth rejection, not-found/ownership-failure, happy path), suite now 21/21; full browser walkthrough via Playwright verified against the Neon `development` branch using a disposable item inserted directly via Neon MCP (`test-delete-item-001`) — confirmed Cancel dismisses with no changes and the item stays intact, confirmed the real Delete path closes the drawer and drops the item from both the card grid and the sidebar count, and confirmed the row was actually gone from Postgres via a direct follow-up query (no manual cleanup needed since the delete itself was the cleanup).
- 2026-07-25 - Item Create (the "New Item" button in `Topbar`, previously a non-functional placeholder, now opens a new `NewItemDialog` — a shadcn `Dialog` with a `Select` type picker for snippet/prompt/command/note/link (file/image excluded, Pro-only) and fields shown per type: Title/Description/Tags always, Content for snippet/prompt/command/note, Language for snippet/command, a required URL for link; Create is disabled until required fields are filled; on `/items/[type]` the type selector defaults to the current page's type by checking the resolved `itemType.name` against a small creatable-names set, computed in the page and passed down as `defaultItemType`; new ownership-scoped `createItem` Server Action in `src/actions/items.ts`, Zod-validated with a `.refine` requiring `url` when `type === "link"`, that resolves the `ItemType` row by reusing the existing `getItemTypeBySlug` lookup (`` `${type}s` `` matches the slug convention `typeHref` already produces) rather than writing a new name-lookup query, and maps `contentType` to `URL` for links / `TEXT` otherwise before calling a new `createItem` query in `src/lib/db/items.ts` that connects/creates tags and returns the new `ItemDetail`; added shadcn `Dialog` and `Select` components) implemented on `feature/item-create`, merged to `master`; build and lint passing, `src/actions/items.test.ts` gained 6 cases (auth rejection, empty title, missing URL for link, unresolvable item type, TEXT-type happy path, URL-type happy path), suite now 27/27; full browser walkthrough via Playwright verified against the Neon `development` branch — created a disposable Note from `/items/notes` (type defaulted to "note", content/tags round-tripped correctly through the item drawer) and a disposable Link from `/items/links` (type defaulted to "link", Create stayed disabled until the required URL was filled), both appeared immediately in the grid and sidebar counts with no console errors; both test items cleaned up afterward via the existing delete flow and confirmed gone from Postgres via a direct query.
- 2026-07-25 - New Item Type Select Styling (in `NewItemDialog`'s Type select, each option (snippet/prompt/command/note/link) now shows its icon tinted with that type's brand color — matching the sidebar navigation exactly (`Code` blue, `Sparkles` purple, `Terminal` orange, `StickyNote` yellow, `Link` green), reusing the canonical icon/color pairs already established in `prisma/typesystem.ts` — rendered both in the dropdown list (`SelectItem`) and next to the currently selected value in the trigger, the latter via base-ui's `Select.Value` function-as-children API (`<SelectValue>{(value) => ...}</SelectValue>`) rather than a static label; removed the `className="w-full"` override on `SelectTrigger`, restoring the shadcn default `w-fit` so the trigger visibly shrinks/grows to match the selected value's width) implemented on `feature/new-item-type-select-styling`, merged to `master`; build and lint passing, no test changes (pure visual change, no server/data logic touched); browser-verified via Playwright — dropdown showed all 5 correctly colored icons, and switching the selection from Snippet to Link visibly narrowed the trigger and correctly swapped the form fields (URL replacing Content/Language). Followed the user's own real-time spec edit mid-conversation (they revised the loaded description from a generic "colored tag" to "icon tag... as navigation already [does]" before `/feature start`), which resolved an ambiguity flagged in the original load about what "tag" meant.
- 2026-07-25 - Code Editor (Monaco) (new `CodeEditor` component in `src/components/items/CodeEditor.tsx` wrapping `@monaco-editor/react` with a `vs-dark` theme, macOS-style red/yellow/green traffic-light dots, a language label, and a copy button in its header; height is fluid up to a 400px cap via `editor.onDidContentSizeChange`, with a themed translucent scrollbar added to `globals.css`; new `toMonacoLanguage` helper in `src/lib/monaco-language.ts` normalizes free-text language values (`ts`, `py`, `sh`, ...) to Monaco language IDs, falling back to `plaintext`; wired into `ItemDrawer` for both readonly display and edit modes, and into `NewItemDialog`'s Content field, but only for snippet/command items — prompt/note/other types keep the existing `Textarea` unchanged) implemented on `feature/code-editor`, merged to `master`; build and lint passing, `npm run test` 27/27 (no new tests — pure UI/component change, no server-action or utility logic in scope per `/feature test` besides the trivial `toMonacoLanguage` mapper); full browser walkthrough via Playwright verified against the Neon `development` branch — readonly Monaco correctly syntax-highlighted a Dockerfile snippet with the "dockerfile" language tag; edit mode confirmed genuinely editable (typed a character, saw it register) and Cancel discarded the change cleanly; New Item dialog showed the same editor for Snippet and correctly fell back to a plain `Textarea` with no dots/language field when switching type to Prompt. Used `@monaco-editor/react`'s default CDN-loaded Monaco rather than bundling `monaco-editor` locally, avoiding any Turbopack/webpack worker configuration.
- 2026-07-25 - Type-Specific Add Button (each `/items/[type]` page now shows a "New {Type}" button (e.g. "New Snippet") in its header next to the heading, and the same action appears as a CTA in the empty state, both opening the New Item dialog preselected to that page's type by reusing the existing `defaultItemType` plumbing rather than rebuilding it; `NewItemDialog` gained a `triggerLabel` prop, defaulting to "New Item", so its trigger text can be customized per instance without duplicating the dialog's form logic; `ItemGrid` gained an optional `emptyAction` slot rendered below the empty-state message; both new buttons are conditional on `defaultItemType`, so File/Image pages are naturally excluded since those types aren't creatable yet; the Topbar's generic "New Item" button is unchanged) implemented on `feature/type-specific-add-button`, merged to `master`; build and lint passing, `npm run test` 27/27 (no new tests — pure UI wiring, no server-action or utility logic touched); full browser walkthrough via Playwright verified — `/items/snippets` header's "New Snippet" button opens the dialog preselected to Snippet, `/items/notes` (empty) shows both the header button and an empty-state "New Note" CTA, `/items/files` (Pro-only) correctly shows neither, `/items/links` header shows "New Link". Loaded as an inline description (not a spec file) via `/feature load`; one open question about the empty-state CTA's styling was flagged in Notes and defaulted to a button matching the header one since the user didn't amend it before `/feature start`.
- 2026-07-25 - Markdown Editor (new `MarkdownEditor` component in `src/components/items/MarkdownEditor.tsx` with custom Write/Preview tab buttons — not the shadcn `Tabs` primitive, whose default pill styling didn't fit `CodeEditor`'s dark header chrome, so a shadcn `tabs.tsx` was added then removed in favor of matching header-button styling — using `react-markdown` + `remark-gfm` (both newly installed) for GFM rendering; header/body match `CodeEditor`'s `bg-[#2d2d2d]`/`bg-[#1e1e1e]` theme with the same copy-button style; readonly mode shows only a "Preview" label (no tabs), edit mode defaults to Write with Preview available; height auto-grows via `textarea.scrollHeight` (write) or CSS `max-height`+scroll (preview), capped at 400px like `CodeEditor`; new `.markdown-preview` CSS class in `globals.css` styling headings/code blocks/inline code/lists/blockquotes/links/tables, plus a shared `.devstash-scroll` translucent scrollbar class reused from the editor styling; wired into `NewItemDialog` and both `ItemDrawer` view/edit modes by replacing the `Textarea`/`pre` fallback branches, since the existing `showsContent && !showsLanguage` condition already isolates exactly Note/Prompt with no new type set needed) implemented on `feature/markdown-editor`, merged to `master`; build and lint passing, `npm run test` 27/27 (no new tests — pure UI/component change, no server-action or utility logic in scope); full browser walkthrough via Playwright verified — created a disposable Note with headings/bold/link/list/blockquote/inline-code/fenced-code/table, confirmed Preview rendering in the New Item dialog, confirmed the drawer's readonly view (Preview-only) and edit mode (Write default, Preview toggle, editable), confirmed Prompt uses the same editor, confirmed Snippet still uses the untouched Monaco `CodeEditor`, then deleted the disposable test item.
- 2026-07-25 - File Upload with Cloudflare R2 (new `POST /api/upload` validates extension/MIME/size via new `src/lib/file-upload.ts` — extension is the authoritative gate, MIME is a soft sanity check since browsers often report empty/`application/octet-stream` for less-common extensions like `.yaml`/`.toml`/`.ini` — then streams to R2 via new `src/lib/r2.ts` (`@aws-sdk/client-s3`, newly installed, using the R2 credentials already present in `.env`); new `FileUpload` component (`src/components/items/FileUpload.tsx`) with drag-and-drop, click-to-browse, real upload progress via `XMLHttpRequest`'s `upload.onprogress` (the Fetch API has no progress events), and a post-upload preview (image thumbnail or file-info card with a remove button); `NewItemDialog` gained File/Image as real creatable types (icons/colors matching the sidebar) wired to `FileUpload`, and `CREATABLE_TYPE_NAMES` in `items/[type]/page.tsx` now includes File/Image so their pages show "New File"/"New Image" buttons — un-gating the functionality for all users while leaving the sidebar's cosmetic "PRO" badge in place, per the project's dev-mode plumbing-gated-but-unlocked convention; `ItemDrawer` view mode shows a full image preview or a file-info card (name/size) plus a Download button in the action bar, streaming through new `GET /api/download/[id]` (ownership-checked via the existing `getItemDetail`, avoids CORS on direct R2 links); `deleteItem` in `lib/db/items.ts` now returns the deleted item's `fileUrl` (via `findFirst` + `delete` instead of `deleteMany`, needed to retrieve the file key) so the `deleteItem` Server Action can clean up the matching R2 object, keeping R2 concerns out of `lib/db/items.ts` per the spec's "stick to lib/db/items.ts for prisma/db functions" instruction; `ItemDetail`/`CreateItemData` extended with `fileSize`/`fileUrl`/`fileName`) implemented on `feature/file-image-upload`, merged to `master`; build and lint passing, `src/lib/file-upload.test.ts` (10 new tests for `validateUpload`/`formatFileSize`) and `src/actions/items.test.ts` gained 5 cases (file-item-with-no-upload rejection, image happy path, R2-cleanup-on-delete with and without a file), suite now 40/40; full browser walkthrough via Playwright verified against the Neon `development` branch — uploaded a real 472KB PNG as an Image item (thumbnail preview in the dialog, full preview in the drawer, byte-perfect download round-trip confirmed via file size match) and a `.md` file as a File item (file-info card, download), deleted both via the existing delete flow and confirmed via a direct Neon query that no File/Image rows remained. Caught and fixed a Base UI console warning along the way: `Button` rendered via the `render` prop as an `<a>` needs `nativeButton={false}`, since Base UI's `Button` defaults to expecting a native `<button>` in its render target.
- 2026-07-25 - File List View (`/items/files` now renders a new single-column `FileList`/`FileListRow` list — icon by extension, title, filename, formatted size, upload date, Download button — instead of the shared `ItemGrid`; every other type, including `/items/images`, keeps the grid unchanged via an `itemType.name === "File"` branch in `items/[type]/page.tsx`; new `src/lib/file-icon.ts` classifies a filename into a `code`/`text`/`spreadsheet`/`generic` category (6 unit tests) with a static `FILE_ICON_BY_CATEGORY` record indexed at the JSX call site, mirroring the existing `TYPE_ICONS[...]` pattern — a first pass called a `getFileIcon()` function directly in the icon assignment and hit an ESLint `react-hooks/static-components` violation ("Cannot create components during render"), since the rule flags CallExpressions but allows MemberExpression/index lookups, so the extension logic was split into a pure category function plus a static lookup table; `DashboardItem`/`toDashboardItem` in `lib/db/items.ts` extended with `fileName`/`fileSize` to support the row display; row click opens `ItemDrawer` via the same `role="button"`/keyboard pattern used by `ItemCard`/`ItemList`, Download button calls `event.stopPropagation()` so it downloads via the existing `/api/download/[id]` proxy without also opening the drawer; row info stacks vertically below the `sm:` breakpoint for mobile) implemented on `feature/file-list-view`, merged to `master`; build (after clearing a stale `.next` dir that hit a transient Windows `EPERM` unlink error) and lint passing, `npm run test` 46/46 (6 new tests for `getFileIconCategory`); full browser walkthrough via Playwright verified against the Neon `development` branch — uploaded a `.txt` and a `.csv` as disposable File items, confirmed the list layout/icons/size/date, confirmed row-click opens the drawer, confirmed the Download button's `stopPropagation` (real file downloaded, drawer stayed closed), confirmed mobile stacking at 390px, confirmed `/items/snippets`'s grid is unaffected, deleted both test items and confirmed via a direct Neon query that no File rows remained.
- 2026-07-25 - Image Gallery View (`/items/images` now renders a new 3-column `ImageGallery`/`ImageThumbnailCard` gallery instead of the shared `ItemGrid`; every other type, including `/items/files`, is unaffected — `items/[type]/page.tsx` refactored from a two-way ternary to a small IIFE dispatching between `FileList`/`ImageGallery`/`ItemGrid` by `itemType.name` while sharing the empty-message/empty-action logic; each thumbnail uses a 16:9 `aspect-video` container with `object-cover` (crops tall/wide originals) and a 5% hover zoom on a 300ms transition, verified via computed styles — Tailwind v4 applies the scale utility through the standalone CSS `scale` property rather than `transform`, so `getComputedStyle(img).transform` reads `"none"` even while hovered; `getComputedStyle(img).scale` is the one to check; title/date footer and pin/favorite indicators kept for consistency with `ItemCard`; row click opens `ItemDrawer` via the same `role="button"`/keyboard pattern used elsewhere; `DashboardItem`/`toDashboardItem` in `lib/db/items.ts` extended with `fileUrl` to support the thumbnail) implemented on `feature/image-gallery-view`, merged to `master`; build (cleared `.next` first to avoid the known transient Windows `EPERM` issue) and lint passing, `npm run test` 46/46 (no new tests — pure UI/component change plus a trivial field pass-through, consistent with prior visual-only features per `/feature test`); full browser walkthrough via Playwright verified against the Neon `development` branch — uploaded two real images, confirmed the 3-column grid/aspect ratio/object-cover cropping tall screenshots, confirmed `object-fit: cover` and exactly `scale: 1.05` on hover with a 300ms transition, confirmed click-to-open-drawer, confirmed `/items/files`'s list view and empty state are unaffected, deleted both test items and confirmed via a direct Neon query that no Image rows remained. Preceded by user correcting a typo/misload — `/feature load` was first run against the already-shipped `file-image-spec.md` (File Upload with Cloudflare R2) before the user clarified they meant `image-display-spec.md`.
- 2026-07-25 - Add Copy Icon to Item Cards (`ItemCard` now shows a `Copy` icon button next to the pin/favorite indicators, always visible when the item has copyable text, clicking calls `event.stopPropagation()` so it doesn't also open the drawer, then `navigator.clipboard.writeText` with the same success toast pattern already used by `ItemDrawer`'s Copy action; resolved the open question in favor of extending `DashboardItem`/`toDashboardItem` in `src/lib/db/items.ts` with `content`/`url` fields so the card can copy client-side with no extra fetch per click; copy target is `item.content ?? item.url`, covering snippet/prompt/command/note (content) and link (url), with File/Image naturally excluded since neither field is set for those and the button only renders when truthy) implemented on `feature/add-copy-icon-item-cards`, merged to `master`. No testable server-action/utility logic in scope (a data pass-through plus a UI-only click handler).
- 2026-07-26 - Decompose ItemDrawer (the 512-line `ItemDrawer` component split into four focused pieces with no behavior change: new `useItemDrawerData(itemId, open)` hook in `src/hooks/use-item-drawer-data.ts` covering the fetch effect + loading/item derivation plus a `setItem` setter for post-save updates; new `ItemDrawerActions` component for the header + action bar (icon/title/badges, Favorite/Pin/Copy/Download, Edit toggle, Delete confirm `AlertDialog`); new `ItemEditForm` component for the edit-mode Title/Description/Content/Language/URL/Tags fields; new `ItemViewContent` component for the view-mode description/URL/file/content/tags sections; the Collections and Details sections stayed directly in `ItemDrawer` since they render in both edit and view mode, unlike the rest of the original ternary, so folding them into `ItemViewContent` would have been a real behavior change) implemented on `feature/decompose-item-drawer`, merged to `master`; build and lint passing, `npm run test` 46/46 unchanged (pure refactor, no server-action/utility logic touched); full browser walkthrough via Playwright verified against the Neon `development` branch — view mode (a Snippet with Monaco content + language badge), edit-mode save round-trip (title change reflected in the drawer, the underlying card list, and the Updated date, then reverted) and Cancel, the Copy action's toast, a Link item's URL section, and a disposable Image item's Download button + image preview + delete-confirm dialog (created and cleaned up via the existing create/delete flow, confirmed gone from the grid afterward), no console errors. Source: code-scanner audit's "break up large blocks" pass; the follow-up (extracting a shared `ItemContentFields` component to de-duplicate `NewItemDialog` and `ItemDrawer`'s edit form) is queued as the next feature.
- 2026-07-26 - Shared ItemContentFields Component (new `ItemContentFields` component in `src/components/items/ItemContentFields.tsx` bundling the conditional Content (CodeEditor or MarkdownEditor depending on `showsLanguage`) + Language `Input` block, previously duplicated almost verbatim in `NewItemDialog` and `ItemEditForm`; both wired to use it in place of their inline blocks. Deliberately scoped to Content+Language only, not URL as originally described when the feature was loaded — `NewItemDialog`'s field order is URL → File → Content → Language while `ItemEditForm`'s is Content → Language → URL, so folding URL into the same fixed-order component would have silently reordered fields in one of the two forms; URL stays as the small inline block already in each file, unchanged) implemented on `feature/shared-item-content-fields`, merged to `master`; build and lint passing, `npm run test` 46/46 unchanged (pure refactor, no server-action/utility logic touched); full browser walkthrough via Playwright verified against the Neon `development` branch — created a disposable Snippet through the New Item dialog with a Monaco-edited body and a "python" language value, confirmed both round-tripped into the drawer, then edited the language to "typescript" in the drawer's edit mode (also using the shared component) and confirmed the change persisted; confirmed a Link item's standalone URL field still renders in its original position with no Content/Language shown; no console errors; disposable test item deleted and confirmed gone. Completes the two-part cleanup queued from the code-scanner audit's "break up large blocks" pass, alongside the preceding Decompose ItemDrawer feature.
- 2026-07-26 - Collection Create (a "New Collection" button added to `Topbar` next to the existing "New Item" button, opening a new `NewCollectionDialog` — a shadcn `Dialog` with Name (required) and Description (optional) fields, Create disabled until Name is filled, matching `NewItemDialog`'s structure; new ownership-scoped `createCollection` Server Action in a new `src/actions/collections.ts`, Zod-validated, mirroring `createItem`'s shape; new `createCollection` query in `src/lib/db/collections.ts` scoped by `userId`; on success the dialog closes with a success toast and calls `router.refresh()`, on failure an error toast and the dialog stays open) implemented on `feature/collection-create`, merged to `master`; build and lint passing, new `src/actions/collections.test.ts` (4 tests: auth rejection, empty-name rejection, happy path, description pass-through) brings the suite to 50/50; full browser walkthrough via Playwright verified against the Neon `development` branch — Create correctly disabled with an empty name, enabled once filled, success toast fired, the new collection appeared immediately in both the sidebar's Recent Collections and the dashboard's collections grid with the right description and "0 items" count, no console errors; disposable test collection deleted via Neon MCP (with explicit user go-ahead per the tool's destructive-SQL confirmation requirement) and confirmed gone from both Postgres and the UI. Two decisions carried from `/feature load` without objection: used a Server Action (not an API route) for the mutation to match "follow the same patterns as items," since the established Item Create flow doesn't use an API route for its mutation; and left the pre-existing demo-user read-scoping gap in `getRecentCollections`/etc. unfixed — `createCollection` is scoped to the real session user, but the sidebar only reflects it when signed in as `demo@devstash.io`, consistent with the same gap already tracked on the items side. No collection detail/listing pages touched, per the spec.
- 2026-07-26 - Session-Scoped Item Reads (fixed a reported bug where clicking an item card showed "Couldn't load this item." for any account other than `demo@devstash.io`: `getPinnedItems`, `getRecentItems`, `getItemStats`, `getItemsByType`, and `getItemTypesWithCounts` in `src/lib/db/items.ts` no longer call the hardcoded `getDemoUser()`/`DEMO_USER_EMAIL`, now removed — each takes a `userId` param instead; `DashboardPage` and `items/[type]/page.tsx` now `await auth()` first, redirect to `/sign-in` if unauthenticated, and thread `session.user.id` through, matching the pattern `getItemDetail`/`/api/items/[id]` already used) implemented on `feature/session-scoped-item-reads`, merged to `master`; build, lint, and `npm run test` 50/50 passing (no new tests — pure parameter threading through existing `src/lib/db/*.ts` queries, consistent with this codebase's precedent of not unit-testing that layer); full browser walkthrough via Playwright verified against the Neon `development` branch — re-confirmed no regression for the demo account (item grid/drawer unchanged), then registered a disposable non-demo account (`bugrepro-test@devstash.io`), confirmed it correctly saw `0` items everywhere (previously it would have shown the demo user's items), created a snippet as that account, confirmed it appeared in its own Recent Items and opened successfully in the drawer with no error, then deleted both the test item and the test account. Deliberately left `src/lib/db/collections.ts`'s identical `getDemoUserId()` pattern (`getRecentCollections`/`getFavoriteCollections`/`getCollectionStats`) unfixed, since the user's report was specifically about items — flagged as a likely follow-up.
- 2026-07-26 - Add Item to Collection(s) (new "Collections" multi-select field added to both `NewItemDialog` and `ItemEditForm`, backed by a new shadcn `checkbox.tsx` (added via the shadcn CLI, first checkbox-style component in this codebase) and a new `CollectionCheckboxList` component rendering a scrollable checkbox list; new `getAllCollections(userId)` in `src/lib/db/collections.ts` and a new `listCollections()` Server Action in `src/actions/collections.ts` feed the list, fetched lazily via a new `useCollections(enabled)` hook — `NewItemDialog` fetches on dialog open, `ItemDrawer` fetches only once editing starts, avoiding an unconditional fetch on every page load; `createItem`/`updateItem` in `src/lib/db/items.ts` gained a `collectionIds: string[]` field on `CreateItemData`/`UpdateItemData`, resolved through a new `resolveOwnedCollectionIds(userId, collectionIds)` helper that silently drops any id not owned by the session user before writing `ItemCollection` rows — closing off a cross-user linking risk the naive nested-write approach would otherwise have allowed; update reconciles membership via `deleteMany: {}` + `create` (mirroring the existing tags `set: []` + `connectOrCreate` pattern) so added/removed/unchanged collections all resolve correctly; `createItem`/`updateItem` Server Actions in `src/actions/items.ts` gained a `collectionIds: z.array(z.string()).default([])` field passed straight through; `ItemDrawer`'s existing read-only Collections badge section is now hidden while editing to avoid showing a duplicate static list alongside the new editable picker) implemented on `feature/add-item-to-collections`, merged to `master`; build and lint passing, `src/actions/collections.test.ts` gained 2 cases (auth rejection, happy path) and `src/actions/items.test.ts` gained 2 cases (collection-id pass-through for both create and update), suite now 54/54 (no new tests for `resolveOwnedCollectionIds`'s ownership-filtering itself, consistent with this codebase's precedent of not unit-testing `src/lib/db/*.ts`); full browser walkthrough via Playwright verified against the Neon `development` branch — created a disposable Snippet from the New Item dialog with two collections checked, confirmed both linked via a direct Neon query on `ItemCollection`, opened the drawer to confirm the read-only badges matched, edited to uncheck one and check a different one, saved, and confirmed via both the drawer UI and a follow-up Neon query that membership reconciled to exactly the new set; deleted the test item via the existing delete flow and confirmed cascade-deleted `ItemCollection` rows via a final Neon query. No collection detail/listing pages touched, per the spec.
- 2026-07-26 - Collections List & Detail Pages (new `/collections` page listing all of the signed-in user's collections via new `getCollectionsForUser(userId)` in `src/lib/db/collections.ts`; new `/collections/[id]` page showing the items in that collection via new `getCollectionById(id, userId)` (ownership-scoped, 404s if not found) and new `getItemsByCollection(userId, collectionId)` in `src/lib/db/items.ts`, rendered with the existing `ItemGrid`/`ItemCard` rather than a new card design; extracted a shared `CollectionCard` component (`src/components/collections/`) from the dashboard's `RecentCollections`' previously-inline `Card` markup, now wrapped in a `Link` to `/collections/[id]` and reused by both the new `/collections` grid (via a new `CollectionGrid`) and `RecentCollections`, so every collection card in the app now links to its detail page; the sidebar's "View all collections" link already pointed at `/collections`, no change needed there) implemented on `feature/collections-list-detail-pages`, merged to `master`; build, lint, and `npm run test` 54/54 passing (no new tests — pure query/UI wiring, consistent with this codebase's precedent of not unit-testing `src/lib/db/*.ts`); full browser walkthrough via Playwright verified against the Neon `development` branch — `/collections` listed all 5 seeded collections with correct item counts, clicking "Design Resources" opened its detail page showing its 4 items in the standard item grid, clicking an item opened the drawer correctly, dashboard's `RecentCollections` cards were confirmed as links, and an unknown collection id 404s. New queries were written session-scoped from the start per the spec's explicit note not to reintroduce the hardcoded-demo-user pattern still used by `getRecentCollections`/`getFavoriteCollections`/`getCollectionStats` (left unfixed, same pre-existing tech debt tracked since the 2026-07-26 Session-Scoped Item Reads entry).
- 2026-07-26 - Collection Edit, Delete & Favorite Actions (`/collections/[id]`'s header gained an action bar — new `CollectionDetailActions` — with a disabled Favorite stub (matching `ItemDrawerActions`' existing Favorite/Pin precedent), Edit (opens a new controlled `EditCollectionDialog` prefilled with name/description), and Delete (opens a new controlled `DeleteCollectionDialog`, then `router.push`es back to `/collections` since the detail page no longer exists after deletion); collection cards (`CollectionCard`, used on `/collections` and the dashboard's `RecentCollections`) gained a 3-dot `CollectionActionsMenu` dropdown with the same Edit/Delete/Favorite, reusing the same two dialog components, `router.refresh()`ing in place afterward instead of navigating away; `CollectionCard` converted from wrapping in `<Link>` to an `onClick`/`useRouter().push` pattern (like `ItemCard`) since a `DropdownMenuTrigger` can't legally nest inside an `<a>`; new ownership-scoped `updateCollection`/`deleteCollection` in `src/lib/db/collections.ts` and matching Zod-validated Server Actions in `src/actions/collections.ts`; deleting a collection only removes it and its `ItemCollection` join rows via Prisma's existing cascade — items themselves are untouched, verified directly against Postgres) implemented on `feature/collection-edit-delete-favorite-actions`, merged to `master`; build, lint, and `npm run test` 61/61 passing (`src/actions/collections.test.ts` gained 7 cases: auth rejection/empty-name/not-found/happy-path for both `updateCollection` and `deleteCollection`); full browser walkthrough via Playwright verified against the Neon `development` branch — edited a real collection's description via the card dropdown and reverted it, created and deleted a disposable collection from both the detail page's action bar and a card dropdown, and round-tripped an existing item through a disposable collection to confirm via direct Neon queries that deleting the collection left the item and its other collection membership fully intact. Caught and fixed a real bug along the way: React bubbles clicks from portaled content (dialogs, dropdown menus) through the *React* component tree rather than the DOM tree, so without a `stopPropagation` wrapper around `CollectionActionsMenu`'s contents, clicking Save/Delete inside a dialog opened from a card's dropdown was also triggering that card's own click-to-navigate handler — first caught by an unexpected URL change after clicking Save in manual browser testing.
- 2026-07-26 - Global Search / Command Palette (Cmd/Ctrl+K opens a search palette from any of the four shell pages — `/dashboard`, `/items/[type]`, `/collections`, `/collections/[id]` — via new `CommandPaletteProvider`/`useCommandPalette` context (mirroring `ItemDrawerProvider`'s pattern) mounted alongside `ItemDrawerProvider` on each; the shadcn `Command`/`CommandDialog` component and its `cmdk` dependency were newly added via the shadcn CLI; searchable data (items: id/title/itemType; collections: id/name/itemCount) is fetched once per page mount from a new session-scoped `GET /api/search-data` route backed by new lightweight `getSearchableItems`/`getSearchableCollections` queries in `src/lib/db/items.ts`/`collections.ts`, deliberately leaner than the existing per-type/heavier collection queries; results render grouped into Items (type-colored icon) and Collections (item count) sections; selecting an item closes the palette and opens its drawer via the shared `ItemDrawerProvider`, selecting a collection navigates to `/collections/[id]`; Topbar's previously-inert readonly search `Input` now opens the palette on click and its placeholder reads "Search items... ⌘K") implemented on `feature/global-search-command-palette`, merged to `master`; build, lint, and `npm run test` 61/61 unchanged (no new server-action/utility logic — pure query/API-route/UI wiring, consistent with this codebase's precedent). Two bugs caught and fixed during browser testing: (1) this codebase's `CommandDialog` variant (shadcn "base-nova" style) does not auto-wrap children in the `<Command>` root provider the way older shadcn versions do, so `CommandInput`/`CommandList`/`CommandItem` had no store context and threw `Cannot read properties of undefined (reading 'subscribe')` the instant the palette opened — fixed by explicitly wrapping the palette's contents in `<Command>`; (2) per user feedback after using it, cmdk's default filter does fuzzy/subsequence matching (letters matching in order but not contiguously, e.g. "test" fuzzy-matching "Common Git Operations Everyday git branch/commit/rebase workflow." via scattered t-e-s-t across title+description) rather than literal substring matching, and the match text included each item's hidden description alongside its visible title — both together produced results with no visible connection to the typed query, so the filter was changed to a plain case-insensitive substring check (`Command`'s `filter` prop) and items now match on title only, trading away description-based matching for predictability. Full browser walkthrough via Playwright verified against the Neon `development` branch — Ctrl+K opened the palette from multiple pages, substring filtering confirmed correct (no results for "test", exactly the three Docker-titled items for "docker"), arrow-key navigation, selecting an item opened its drawer with no console errors, selecting a collection navigated correctly, Escape closed cleanly.
- 2026-07-26 - Settings Page (new protected `/settings` route added to `proxy.ts`'s `PROTECTED_PATH_PREFIXES`/`matcher`; `AppSidebar`'s user dropdown gained a "Settings" link (lucide `Settings` icon) between Profile and Sign out; the profile page's Account card — Change Password (`ChangePasswordForm`, shown only when `user.hasPassword`) and Delete Account (`DeleteAccountDialog`) — moved from `/profile` to the new `src/app/settings/page.tsx`, reusing `getProfileUser(session.user.id)` from `src/lib/db/profile.ts` with no new query; `/profile` keeps only its avatar/name/email/member-since and Usage/item-type-breakdown cards) implemented on `feature/settings-page`, merged to `master`; build, lint, and `npm run test` 61/61 unchanged (pure UI/route wiring, no new server-action/utility logic); full browser walkthrough via Playwright verified against the Neon `development` branch — unauthenticated `/settings` redirected to `/sign-in?callbackUrl=...` via curl, `/profile` no longer showed the Account card, sidebar dropdown's new Settings entry navigated to `/settings`, and the moved `ChangePasswordForm`/`DeleteAccountDialog` still functioned correctly there (wrong-current-password error re-verified, delete confirmation dialog opened and was cancelled without deleting), no console errors. The spec's "delete account and forgot password" was interpreted as Change Password + Delete Account, since `/profile` never had a forgot-password link (that flow lives separately on `/sign-in` → `/forgot-password` for logged-out recovery) — flagged to the user during `/feature load`, no correction given before `/feature start`.
- 2026-07-26 - Editor Preferences Settings (new "Editor Preferences" card on `/settings` — Font Size, Tab Size, Theme (vs-dark/monokai/github-dark), Word Wrap, and Minimap, all auto-saving with no Save button, backed by a new `editorPreferences Json?` column on `User` (migration `20260726194009_add_editor_preferences_to_user`), new `getEditorPreferences`/`updateEditorPreferences` in `src/lib/db/settings.ts`, a Zod-validated `updateEditorPreferences` Server Action in `src/actions/settings.ts`, and new client-safe types/defaults in `src/lib/editor-preferences.ts` split out specifically so the client-side `EditorPreferencesProvider` context never imports the Prisma-touching `src/lib/db/settings.ts`; `CodeEditor` now reads live preferences instead of its old hardcoded `fontSize: 13`/`minimap: false`, and applies `wordWrap`/`theme` too; new `src/lib/monaco-themes.ts` registers "monokai" and "github-dark" as real Monaco themes via `monaco.editor.defineTheme()` since neither ships built-in; added shadcn `switch` component) implemented on `feature/editor-preferences-settings`, merged to `master`; build and lint passing, `src/actions/settings.test.ts` (5 new tests: auth rejection, invalid font/tab size, invalid theme, happy path) brings the suite to 66/66; full browser walkthrough via Playwright verified against the Neon `development` branch — changed theme/font-size/minimap on `/settings`, confirmed a success toast per change, confirmed values persisted across a reload, then confirmed via computed styles that a snippet's Monaco editor on a completely different page (`/items/snippets` drawer and the New Item dialog) picked up the same live preferences (monokai → `rgb(39,40,34)` background, github-dark → `rgb(13,17,23)`, 20px font, minimap rendered), demo account's preferences reset back to defaults afterward. Caught and fixed a real regression mid-feature: the original plan to mount `EditorPreferencesProvider` in the root layout made `auth()` run on every request, which silently dropped static generation for previously-static public pages (`/`, `/register`, `/forgot-password` all flipped from `○` to `ƒ` in the build output) — fixed by reverting `src/app/layout.tsx` untouched and instead fetching preferences + mounting the provider individually in the 5 pages that actually need it (`/dashboard`, `/items/[type]`, `/collections`, `/collections/[id]`, `/settings`), all of which were already `force-dynamic` and already calling `auth()`, so the fix added one query to an existing `Promise.all` with zero new cost and restored the public pages' static generation (confirmed via build output before/after). Also hit and fixed a related bundling error: importing `getEditorPreferences`/`DEFAULT_EDITOR_PREFERENCES` together from one Prisma-touching module into a `"use client"` file pulled the whole Prisma client into the browser bundle (`node:module` Turbopack chunking error) — fixed by splitting client-safe types/constants into `src/lib/editor-preferences.ts`, separate from the Prisma-calling `src/lib/db/settings.ts`. Two small follow-up UI tweaks after `/feature complete`-adjacent user feedback: `CardTitle`s on `/settings` bumped to `text-2xl font-bold` to read as real section titles (matching a `text-xl` convention already used on the sign-in/register/forgot-password/reset-password cards), and a `Separator` added directly under each `CardHeader` on both cards.
- 2026-07-27 - Favorites Page (new `/favorites` route listing the signed-in user's favorited items and collections in a compact, monospace list view — separate "Items"/"Collections" sections with counts, each row showing a type icon, title, type badge (items) or item count (collections), and a formatted date, sorted by `updatedAt desc`; reachable via a new star icon button in `Topbar` linking to `/favorites`; route protected via `proxy.ts`'s `PROTECTED_PATH_PREFIXES`/`matcher` plus an in-page `auth()` redirect to `/sign-in`, matching `/dashboard`/`/collections`/`/settings`; new session-scoped `getFavoriteItems(userId)` in `src/lib/db/items.ts` and `getFavoriteCollectionsForUser(userId)` in `src/lib/db/collections.ts`, both written userId-scoped from the start per the spec's explicit note not to reintroduce the hardcoded-demo-user pattern; item rows open the shared `ItemDrawer` via `useItemDrawer`, collection rows navigate to `/collections/[id]`; new `FavoritesSection`/`FavoriteItemRow`/`FavoriteCollectionRow` components) implemented on `feature/favorites-page`, merged to `master`; build (cleared `.next` first to avoid the known transient Windows `EPERM` issue) and lint passing, `npm run test` 66/66 unchanged (no new tests — pure query/UI wiring reusing established session-scoped patterns, consistent with this codebase's precedent of not unit-testing `src/lib/db/*.ts`); full browser walkthrough via Playwright verified against the Neon `development` branch — signed in as `demo@devstash.io`, confirmed "7 favorites" with Items (6) correctly sorted newest-first and Collections (1), clicking an item opened the drawer with correct Monaco-rendered content, clicking the collection navigated to `/collections/[id]`, no console errors. This session picked up mid-feature after a prior session was interrupted (Claude Code page closed) with the implementation already complete but uncommitted on the feature branch — `/feature start` was used to audit and verify the existing work end-to-end (lint/build/test/browser) rather than re-implementing, confirming all spec goals were already met before proceeding straight to `/feature complete`. Also surfaced but left untouched: several orphaned `next dev` processes (ports 3000-3003) left running from the interrupted session, unrelated to this feature.
- 2026-07-27 - Favorite Toggle (Items & Collections) (wires up the previously-disabled "Favorite" star stubs with real toggle functionality across four entry points: `ItemDrawerActions`' drawer button, `CollectionDetailActions`' detail-page button, `ItemCard`'s badge (now a clickable button next to Copy/Pin, same `stopPropagation` pattern), and `CollectionCard`'s badge plus the matching `CollectionActionsMenu` dropdown entry (previously disabled); new ownership-scoped `toggleItemFavorite`/`toggleCollectionFavorite` queries in `src/lib/db/items.ts`/`collections.ts` that read-then-flip `isFavorite` and return the new value, backed by matching Server Actions in `src/actions/items.ts`/`collections.ts`; no schema change since `isFavorite` already existed on both models — this only wires up the write path; all call sites propagate via `router.refresh()`, the codebase's established pattern, so the sidebar's Favorite Collections list and `/favorites` stay in sync automatically) implemented on `feature/favorite-toggle`, merged to `master`; build and lint passing, `src/actions/items.test.ts`/`collections.test.ts` gained 3 cases each (auth rejection, not-found, happy path), suite now 72/72; full browser walkthrough via Playwright verified against the Neon `development` branch — toggled and reverted via all four entry points (item card badge, item drawer, collection card badge, collection dropdown, collection detail page), confirmed each propagated correctly to the sidebar's Favorite Collections list, confirmed card-toggle clicks don't also open the drawer/navigate, no console errors; all test toggles reverted to restore original seed state. Deliberately left Pin toggling and the File/Image card favorite indicators (`ImageThumbnailCard`, `FileListRow`) untouched, per the loaded spec's scoping notes.
- 2026-07-28 - Favorites Page Sorting (new "Sort by" `Select` on `/favorites` with Date/Name/Item Type options, default Date to match the prior fixed `updatedAt desc` behavior; new pure `sortFavoriteItems`/`sortFavoriteCollections` in `src/lib/sort-favorites.ts` — Name sorts alphabetically, Date sorts newest-first, Item Type sorts items alphabetically by `itemType.name` and leaves collections untouched, since a collection has no single item type; new client `FavoritesList` component holds the shared sort state and renders both `FavoritesSection` blocks, replacing the two inline blocks previously in `src/app/favorites/page.tsx`; entirely client-side reordering of already-fetched data, no new query/server-action logic) implemented on `feature/favorites-page-sorting`, merged to `master`; build, lint, and `npm run test` passing, `src/lib/sort-favorites.test.ts` (7 new tests covering all three sort options for both items and collections, plus a no-mutation check) brings the suite to 79/79; full browser walkthrough via Playwright verified against the Neon `development` branch, signed in as `demo@devstash.io` — confirmed Date is the default and matches the prior order, Name sorts items alphabetically (Code Review Prompt → ... → Production Dockerfile), Item Type groups items by type name (Command, Command, Link, Prompt, Snippet, Snippet) while the single-item Collections section stayed unaffected, no console errors. Resolved one ambiguity via `/feature load`'s clarifying question before implementation: since favorite collections carry no single item type, "sort by item type" only reorders the Items section — the user confirmed this over extending the collections query with dominant-type data or adding a second independent sort control per section.
- 2026-07-28 - Pinned Items (`ItemDrawerActions`' previously-disabled Pin button — `disabled title="Coming soon"`, no `onClick` — is now functional, with an optimistic UI update (flips immediately, reverts and shows an error toast on failure) and a success toast on both pin and unpin, a deliberate deviation from the simpler "await-then-update" pattern the Favorite feature used, per the spec's explicit requirements; new ownership-scoped `toggleItemPin` query in `src/lib/db/items.ts` mirroring `toggleItemFavorite`, backed by a matching Server Action in `src/actions/items.ts`; `getItemsByType`/`getItemsByCollection` in `src/lib/db/items.ts` now order by `isPinned desc` then `createdAt desc` (previously `createdAt desc` only) so pinned items float to the top of `/items/[type]` and `/collections/[id]` listings; dashboard's existing "Pinned" section (`getPinnedItems`, hidden when empty) needed no code changes — it started populating automatically once items could actually be pinned; items only, no collection-pin support; `ItemCard`'s pin icon stays a static read-only indicator, unlike Favorite's star which became a clickable toggle — pinning is drawer-only) implemented on `feature/pinned-items`, merged to `master`; build and lint passing, `src/actions/items.test.ts` gained 3 cases (auth rejection, not-found, happy path), suite now 82/82; full browser walkthrough via Playwright verified against the Neon `development` branch — pinned a mid-list Snippet from the drawer, confirmed the "Item pinned" toast, confirmed it jumped to the top of `/items/snippets`'s grid, confirmed it appeared in the dashboard's newly-populated "Pinned" section; unpinned it back via the drawer, confirmed the "Item unpinned" toast and that the dashboard's Pinned section correctly disappeared again (hidden-when-empty), no console errors. Loaded from the pre-existing but previously-uncommitted `context/features/pinned-spec.md`, which is now committed alongside this feature since it directly documents it (unlike in prior sessions where it sat as an unrelated stray file).
- 2026-07-28 - Homepage Mockup (new standalone static marketing prototype at `prototypes/homepage/` — `index.html`/`styles.css`/`script.js`, not wired into the Next.js app — implementing the loaded `context/features/homepage-mockup-spec.md`: fixed nav that grows more opaque on scroll; gradient-text hero headline/subheadline/CTAs; a three-part "chaos to order" hero visual — a chaos container with 8 icon chips (Notion, GitHub, Slack, VS Code, browser tabs, terminal, text file, bookmark) hand-drawn as simple stroke SVGs (not exact brand logos) that drift and bounce off the container walls, pulse via a CSS box-shadow keyframe, and repel from the mouse cursor, driven by a `requestAnimationFrame` physics loop in `script.js`; a CSS-pulsing transform arrow that rotates 90° to point down on mobile; and a dashboard-preview box with a mini sidebar + type-colored mini item cards; a 6-card features grid, an AI section with a Pro badge/checklist and a mock code-editor panel whose "AI Generated Tags" pop in on load; a pricing section (Free vs Pro) with a working monthly/yearly ($8 ↔ $72) toggle and a "Most Popular" badge; a CTA section; and a footer with a JS-set current year; scroll-triggered fade-ins via `IntersectionObserver`) implemented directly via `/feature start` → `/feature complete` on `feature/homepage-mockup`, merged to `master`; no `npm run build`/`lint`/`test` changes since the prototype lives outside the Next.js app and touches no server-action/utility logic; browser-verified via Playwright against a temporary local static server (file:// is blocked by the browser tool) — confirmed the hero visual and mini dashboard render correctly, confirmed chaos icons genuinely animate frame-to-frame, confirmed the pricing toggle correctly swaps $8/mo ↔ $72/yr, and confirmed the 390px mobile layout stacks chaos/arrow/dashboard vertically with the arrow rotated to point down and nav links hidden; only a harmless missing-favicon 404 in the console. Also caught mid-`/feature complete` that `git merge` had created a real merge commit, breaking this repo's established fast-forward-only history — reset and redid it as a fast-forward to match convention.
- 2026-07-28 - Homepage (rebuilt the real `src/app/page.tsx` from the `prototypes/homepage/` mockup, replacing the `<h1>Devstash</h1>` placeholder; new `context/features/homepage-spec.md` spec; new components under `src/components/homepage/` split into server sections (`Features`, `AiSection`, `CtaSection`, `Footer`) and client pieces for interactivity (`NavbarScroll` scroll-opacity wrapper, `HeroVisual` porting `script.js`'s chaos-icon drift/bounce/mouse-repel `requestAnimationFrame` physics loop plus the dashboard-preview mini-grid, `PricingSection` holding the monthly/yearly toggle state, and a reusable `FadeIn` `IntersectionObserver` wrapper used across every section in place of the mockup's single global scroll-reveal script); feature-card accents deliberately use the app's canonical item-type palette from `project-overview.md` (Snippet blue, Prompt purple, Command orange, File gray, plus Link emerald for Instant Search and Note yellow for Collections) rather than the mockup's standalone homepage palette, so they match the sidebar; brand gradient/keyframe CSS (`homepage-gradient-text`, `homepage-gradient-bg`, `homepage-icon-pulse`, `homepage-arrow-pulse`, `homepage-tag-pop`, `homepage-fade-in`) added to `globals.css` under a scoped "Homepage" section; all CTAs/nav links wired to real routes (Sign In → `/sign-in`, Get Started/Upgrade to Pro/CTA → `/register`, Features/Pricing → in-page anchors), footer About/Blog/Privacy/Terms deliberately left as non-navigating `#` since no pages exist for them yet) implemented on `feature/homepage`, merged to `master`; build (`/` statically generated) and lint passing, `npm run test` 82/82 unchanged (no new server-action/utility logic — pure UI/animation work); full browser walkthrough via Playwright verified — hero gradient text/CTAs/chaos animation/dashboard preview all render correctly, pricing toggle swaps `$8/mo` ↔ `$72/yr`, "Get Started" and "Sign In" nav buttons confirmed to land on `/register`/`/sign-in` respectively, 390px mobile layout stacks correctly with the arrow rotated to point down and nav links hidden, no console errors. Along the way, found and killed 3 stale orphaned `next dev` processes on ports 3000/3002/3003 left over from a previous interrupted session (the same ones flagged as tech debt in the 2026-07-27 Favorites Page entry) — they were serving mismatched code and caused confusing scroll/render artifacts during verification; left the unrelated `colo_project` dev server on port 3001 untouched, then re-verified cleanly against a single fresh dev server.